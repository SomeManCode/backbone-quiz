/* Score Model */
define(
    ['backbone'],
    function(Backbone) {
        var ScoreModel = Backbone.Model.extend({
            defaults : {
            	
            }
        });
        
        return ScoreModel;
    }
);