/* Score Model */
define(
    ['backbone'],
    function (Backbone) {

        'use strict';

        var ScoreModel = Backbone.Model.extend({
            defaults : {

            }
        });

        return ScoreModel;
    }
);